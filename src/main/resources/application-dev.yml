server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/ornek

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true

  data:
    mongodb:
      host: localhost
      port: 27000
      username: bilgeadamuser
      password: 'bilgeadam!'
      database: socialmedia
